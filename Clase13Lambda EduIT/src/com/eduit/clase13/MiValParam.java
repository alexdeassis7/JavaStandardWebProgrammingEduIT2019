package com.eduit.clase13;

//Expresión lambda con uno o más parámetros
public interface MiValParam {
//	Si la expresión lambda toma uno o más parámetros, entonces el método abstracto en la interfaz funcional también debe tomar el mismo número de parámetros. 
//	, aquí hay una interfaz funcional llamada MiValParam, que le permite pasar un valor a getValor():
//	
	double getValor(double v);

}
